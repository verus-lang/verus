use vstd::prelude::*;
                                         verus! {                                        struct a<b > {                                       c: b}                                        impl<b > View for a<b> {                                       type V = (Seq<b>, );                                       spec fn view(&self) -> Self::V ;                                   }                                    impl<b > a<b> {                                   fn enqueue(self  ) -> (d: bool)         ensures             (self@.0.len() == self@.0.len()  )== forall |e|                 self@.0[e] == self@.0[e]                        {                                    true         }                                              }                                    fn main() ;                                    }
